# 《星星跟随色块消除》——拍过板的决策

规格原文是对话里的附件，没进仓库。这份文件记的是**已经拍过板、代码按它写的那
几条**，免得只活在聊天记录里。写法照 `外边消除决策.md`。

---

## 一、一组图案值几分：玩家选的「乙」案

代码在 `src/engine/groupScore.ts`（全站唯一一处），门是
`scripts/check-star-score.mjs`（CI 第 38 步）。

    一组里有色块  →  max(4, 色块枚数) + 星星枚数²
    一组全是星星  →  星星枚数²          ← 色块那部分是 0，不是 4

记号：色块 = `face: 'flavor'`（有颜色那面）；星星 = `face: 'dot'`（消过一次、
翻过来那面，界面上就是那颗「*」）。那个 4 是「能得分的最小图案就是 4 格」
（1×4、2+2、1-2-1）的地板。

几个钉住的数：

| 组成 | 分 | 说明 |
| --- | --- | --- |
| 4 色块 | 4 | 和从前一模一样 |
| 6 色块 | 6 | 和从前一模一样 |
| 1×5 同色星星（整组只有它） | **25** | 玩家亲口点名的那个例子 |
| 4 星星 | 16 | 所以规格 §1.3 里那个「20」是笔误 |
| 2 色块 + 3 星星 | 13 | 乙和甲的分水岭（甲是 11） |
| 1 色块 + 4 星星 | 20 | 地板算在色块头上，这是乙的选择 |

### 另外两种读法为什么没选

- **甲**：地板算在整组头上（`max(4, 总枚数) − |S| + |S|²`）。同样满足「纯 5
  星星 = 25」和「纯色块不变」这两个端点，混合那一格算出 11。我当时推荐甲，玩家
  挑了给得更爽的乙。
- **丙**：只要组里有一颗星星，整组一起平方。**否掉**，因为它有断崖和刷分口：
  `●●●★`（3 色块 + 1 星星）会从 4 分跳到 16 分，于是玩家只要往任何图案里塞进一
  颗星星，分就翻四倍。乙没有断崖——多一颗星星只多 `2|S|−1` 分，门里那条
  「往一组里多加一颗星星，分数只增不减」扫了 56 种组合守着这件事。

### 老虎机不受影响

老虎机模式（targets）的图案分走 `src/engine/targets.ts` 的 `scoreOf`
（`ceil(n²/2)`），是另一条路，这次一个字都没碰——它的分早就印在玩家的记录里了。

---

## 二、规格里核出来的六处，逐条的结论

| | 规格怎么写的 | 核实结果 |
| --- | --- | --- |
| 1 | 「七副棋盘」 | **错，是八副**。漏掉了 `squareDiamond`。`src/shapes/` 下八个模块：square squareDiamond circle circleHex circleSeven triangle triangleBig triangleAdvanced |
| 2 | BLANK 清单 | **漏了 `triangle.ts`**。另外 `circle.ts` 和 `triangleBig.ts` 早就有 `isBlank` 了，不是从零开始 |
| 3 | §1.3 纯星星四连一处 16 一处 20 | **自相矛盾**。按玩家「按星星枚数的平方」的说法，是 **16** |
| 4 | `clearedStars` 的 mask | **不对称，会开刷分洞**。玩家说的「这 5 个消除」正好是那道闸：星星得分后**从棋盘上消失**（留空位），所以同一批星星凑不回同一个形状，不需要另记一本账 |
| 5 | 全局删掉 `scoring.ts` 的 flavor filter（「一组里至少有一枚正面才给分」） | **不能全局删**。那条正是防刷分的闸，而且删掉会改到无限反转，跟玩家说的「无限反转维持原样」冲突。删除要挂在 `!cfg.toggleOnMatch` 上 |
| 6 | D8（V 形仅天才挑战）列为作废 | **错，照旧有效**。它和外边消除无关，见 `外边消除决策.md` |

---

## 三、玩家对四个问题的回答（鱼眼转盘那份规格）

1. 手机端**不**循环
2. 焦点锁定开关**先不做**
3. reduced-motion 下：**只在最后选中一个图标停下来的那一刻**出声，快速滑过的时
   候不播
4. 弹簧系数给一个**能被感知到的小数值**；转满一圈**不提示**

另：主菜单是**整个换掉**（不是在现有三排上加转盘）。

---

## 四、还没做的

- **接线只做了一半**：`groupPoints` 已经接上八副棋盘，所以**混合组**（色块里夹
  着星星）现在就按新规则给分了。但**纯星星组还得不到分**——`scoring.ts:290` 那
  道 flavor filter 还挡着它（见上面第 5 条），得连着 BLANK（第 2、4 条）一起做。
- **排行榜要清**。改了记分尺子，旧榜和新榜就不是一把尺子量出来的。工具已经有：
  `public/mint.html` 的《重建榜单》，`drop` 勾掉要清的玩法（当年清《无限反转》
  的旧榜就是用它）。注意 rebuild 是**照存档重新聚合**，不会把旧局按新公式重算，
  所以受影响的玩法得直接 drop 掉。
- 教学文案、动画、记录页面：都还没动。
