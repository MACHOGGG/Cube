# Slides 抖音小游戏上手指南

写在最前面的三句话：

1. **技术上这次很省事。** 抖音小游戏和微信小游戏是同一种东西——没有 DOM，只有
   一块画布，API 从 `wx.` 换成 `tt.`。而 `wxgame/` 里那套骨架已经把「画布 / 触摸 /
   震动 / 下一帧」抽成了一层 `platform.ts`，加抖音就是给它加第三个分支。
2. **真正的瓶颈是资质，不是代码。** ICP 备案 7-20 个工作日，软著 1-2 个月。这两样
   都得排队等，而且**必须先办**——没有它们连提审的入口都进不去。
3. **个人主体做不了内购。** 个人号只能靠广告变现，没有支付权限。要卖「天才」那一
   套，得用企业/个体工商户主体。

> 关于这份文档的来源：抖音开放平台的官方文档站（developer.open-douyin.com）在我
> 这边的网络里打不开（出口代理拦掉了），所以下面的平台规则来自搜索到的二手资料和
> 官方文档的搜索摘要。**真正动手前，每一条都到官方文档核一遍**——尤其是资质清单
> 和审核规范，平台改规则不打招呼。文末列了该看的几页。

---

## 一、先定三件事

### 1. 做「小游戏」，不做「小程序」

和微信那次一样的结论，而且抖音这边更没得选：

| | 抖音小程序 | 抖音小游戏 |
|---|---|---|
| 画界面 | 类 WXML/WXSS | 没有 DOM/BOM，只有 `tt.createCanvas()` 给的一块画布 |
| 能不能内嵌网页 | 有 `web-view` 组件 | **不支持 webview** |
| 个人主体 | **个人类型的小程序不支持 web-view** | 可以做 |

这一栏值得单独说：本来有一条「偷懒路线」——做个小程序，用 `web-view` 把现成的
H5（就是小红书那个包）塞进去。**这条路对我们是堵死的**，两头都堵：小游戏根本没有
webview；小程序的 web-view 个人主体用不了。而且游戏内容用小程序类目提交，按微信那
边的经验大概率被打回。

所以：**老老实实做小游戏，用 Canvas 画**。

### 2. 一份代码，三个平台

`wxgame/src/platform.ts` 现在长这样：

```ts
export function createPlatform(): Platform {
  if (typeof wx !== 'undefined' && typeof wx.createCanvas === 'function') return wxPlatform();
  return browserPlatform();
}
```

加抖音只要变成：

```ts
  if (typeof tt !== 'undefined' && typeof tt.createCanvas === 'function') return ttPlatform();
  if (typeof wx !== 'undefined' && typeof wx.createCanvas === 'function') return wxPlatform();
  return browserPlatform();
```

`ttPlatform()` 和 `wxPlatform()` 几乎逐行对应（`tt.createCanvas`、`tt.onTouchStart`、
`tt.vibrateShort`、`tt.getSystemInfoSync`）。**游戏本体一行都不用改**——它只认
`Platform` 这个接口。

浏览器那一支要留着：`scripts/check-wxgame.mjs` 就是靠它在无头浏览器里跑回归和截
图的，抖音这一版同样能用它把门跑绿。

### 3. 后端：先不做

微信那次的结论是「用微信云开发，因为 `wx.request` 只能访问已备案域名」。抖音同理，
而且抖音的云能力不如微信云开发成熟。

**建议第一版不带后端**：单人玩法 + 本机最高分，一个网络请求都不发。理由：
- 不发请求就不用配服务器域名，少一道审核关卡；
- 多人小屋在抖音上本来就没有社交土壤（不像微信有好友关系）；
- 先用最小的东西过审、看数据，值不值得再做后端由数据说话。

真要做，等 ICP 备案下来之后，`api/` 那套直接部署到国内主机，抖音端调它就行——**备案
本来就是必办的**，办完顺手就把这条路打通了。

---

## 二、资质：这才是关键路径

三样东西，按能不能并行排：

| 要办的 | 周期 | 花钱 | 备注 |
|---|---|---|---|
| **主体认证** | 快，当天到几天 | 个人免费 | 身份证 + 银行卡 + 手机 + 邮箱 |
| **软著**（计算机软件著作权登记证书） | **1-2 个月** | 200-1500 元 | 主体和游戏名必须和小游戏账号**完全一致** |
| **ICP 备案** | **7-20 个工作日** | 免费 | 工信部强制，没有它进不了提审 |
| 游戏自审查报告 | 自己写，一天 | 免费 | 提审时要交 |
| 版号 | — | — | **免费且不开内购的游戏不需要**，靠广告变现即可 |

> 部分平台现在也认「电子版权认证」，比传统软著快。值得先查一下抖音这边认不认——
> 认的话能把那 1-2 个月压掉一大截。

**关键路径 = 软著（1-2 个月）**。代码那边一两周就能出个能玩的版本，所以：

> **今天就该动的是软著和 ICP 备案，不是代码。**

游戏名要先定死——软著、ICP 备案、小游戏账号三处必须一模一样。现在叫「Slides」，
中文名要不要叫「方糖谜题」，得在办软著之前定下来，之后改名等于重办。

---

## 三、现有代码能搬多少

我按「能不能脱离浏览器跑」重新盘了一遍（和微信那次同一套判断，因为环境一样）：

**直接复用，一行不改**（`wxgame/` 里已经在用了）

- `src/engine/scoring.ts` 计分连锁
- `src/engine/stalemate.ts` 死局判定
- `src/engine/targets.ts` / `targetMatch.ts` 老虎机 20 个图案
- `src/engine/rng.ts` 可播种随机数
- `src/engine/performance.ts` 有效得分率
- `wxgame/src/composite.ts` 综合得分公式

**已经写好、抖音直接能用**（微信那次做的，共 4223 行）

| 文件 | 行数 | 是什么 |
|---|---|---|
| `wxgame/src/render.ts` | 463 | Canvas 画棋盘、动画 |
| `wxgame/src/main.ts` | 583 | 主流程：菜单 → 开局 → 结算 |
| `wxgame/src/squareBoard.ts` | 329 | 方块的规则模型（不带画面） |
| `wxgame/src/circleBoard.ts` | 343 | 小球的规则模型 |
| `wxgame/src/menu.ts` | 169 | 主菜单 |
| `wxgame/src/theme.ts` | 118 | 配色 |
| `wxgame/src/platform.ts` | 148 | **要加的就是这个文件里的一个分支** |

**要新写的**

- `ttPlatform()`：约 60 行，照着 `wxPlatform()` 抄，`wx.` 换 `tt.`
- `game.json` 的抖音版：字段名和微信的不完全一样，要查文档
- 抖音的分享（`tt.shareAppMessage`）和录屏分享——抖音这边录屏分享是特色能力，值得用
- 激励视频广告（`tt.createRewardedVideoAd`）：个人主体唯一的变现手段

**不搬**（和微信那次一样）

- 账号、订阅、内部码、Creem 支付——抖音版全部免费
- 多人小屋（第一版不做）

---

## 四、变现的现实

个人主体只能投广告，没有内购。抖音的广告分成（搜到的数字，需自行核实）：

- 首发游戏，广告日流水 100 万以下，开发者拿 **70%**；超过的部分 60%
- 非首发（也就是已经在别家上过的），各降 10 个百分点

「首发」这一条对我们有影响：**如果先上微信再上抖音，抖音这边就算不上首发，分成低
10 个点**。要不要为了这 10 个点调整两边的先后顺序，值得你想一想——不过说实话，日
流水到不了那个量级之前，这 10 个点是纸面上的事。

激励视频的常见接法（都不影响免费体验）：
- 结算页「看个广告，复活/再来一局」
- 主菜单「看个广告，解锁一次进阶玩法试玩」

---

## 五、我建议的顺序

**第 0 周（今天就能开始，不用等代码）**
1. 定死游戏中文名
2. 办软著（最慢的一环，先起跑）
3. 注册抖音开放平台账号，做主体认证
4. 起 ICP 备案

**第 1-2 周（等资质的同时写代码）**
5. `platform.ts` 加 `ttPlatform()` 分支
6. 出抖音版 `game.json` + `project.config.json`
7. 用抖音开发者工具真机跑一遍单人方块 + 小球
8. `scripts/check-wxgame.mjs` 那套门加抖音这一支

**第 3 周起（资质陆续到位）**
9. 接激励视频广告
10. 接抖音的分享 / 录屏分享
11. 填后台资料，交自审查报告，提审

**之后（看数据再决定）**
12. 三角玩法、老虎机、无限反转
13. 后端与多人（要不要做，由第一版的留存数据说话）

---

## 六、动手前要自己核一遍的官方文档

我这边打不开官方站，下面这几页务必你自己过一遍，以官方为准：

- 小游戏文档指引 · `developer.open-douyin.com/docs/resource/zh-CN/mini-game/guide/overview`
- 开发指南（`tt` API、Canvas） · `.../mini-game/develop/guide/dev-guide/bytedance-mini-game`
- **小游戏资质规范** · `.../mini-game/operation1/norms/credential-norms-for-mini-game`
- **小游戏基础信息审核规范** · `.../mini-game/operation1/norms/game-info-audit`
- 小游戏行为规范 · `.../mini-game/operation1/norms/game-rule`
- **ICP 备案指引** · `.../mini-game/guide/minigame/icp-filings`

重点核这三件事：
1. 个人主体到底能不能上线、有没有数量/变现限制（搜到的说法是能上、只能广告、上限 100 个）
2. 软著能不能用「电子版权认证」替代（能的话省一个多月）
3. 免费且无内购是否确实不需要版号
