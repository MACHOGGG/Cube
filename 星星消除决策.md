# 《星星跟随色块消除》——拍过板的决策

规格原文是对话里的附件，没进仓库。这份文件记的是**已经拍过板、代码按它写的那
几条**，免得只活在聊天记录里。写法照 `外边消除决策.md`。

---

## 一、一组图案值几分：最终规则

代码在 `src/engine/groupScore.ts`（全站唯一一处），门是
`scripts/check-star-score.mjs`（CI 第 38 步）。

    一组里有色块  →  max(4, 整组枚数)   ← 和从前**完全一样**，一个数都没变
    一组全是星星  →  星星枚数²

也就是说：**混合组什么都不变**。星星混在色块的图案里，既不多算分、也不消除；消
除的还是原来那件事——色块翻成星星，星星留在原地。只有「整组都是星星」是新的：按
星星枚数平方给分，那一批星星**从棋盘上消除**（所以同一批星星凑不回同一个形状，
不需要另记一本防刷分的账）。

记号：色块 = `face: 'flavor'`（有颜色那面）；星星 = `face: 'dot'`（消过一次、翻过
来那面，界面上那颗「*」）。那个 4 是「能得分的最小图案就是 4 格」（1×4、2+2、
1-2-1）的地板。

玩家给的数，逐条：

| 组成 | 分 | 说明 |
| --- | --- | --- |
| 4 色块 | 4 | 和从前一样 |
| 3 色 1 星 | 4 | 星星不消除，色块变星星 |
| 2 色 3 星 | 5 | 同上，`max(4, 5)` |
| 纯 4 星（1×4） | 16 | |
| 纯 5 星（1×5） | 25 | |
| 纯 6 星（2×3 那一块） | 36 | 平方的是**枚数**，不是边长 |

### 一个故意的「往下掉」

往一组纯星星里塞进一枚色块，分是会掉的：纯 5 星 25 分 → 1 色 5 星 `max(4,6)` = 6
分。这不是 bug，是规则的用意——星星只在**自己单独成图案**的时候才值钱，所以玩家
该去凑纯星星的图案。门里专门钉了这一条，免得哪天有人当成 bug「修」掉。

### 走过的弯路

定稿之前拿三种读法问过玩家（都满足「纯星星 = 枚数²」和「纯色块不变」这两个端点，
只在混合那一格分岔）：

| | 混合怎么算 | 2 色 3 星 |
| --- | --- | --- |
| 甲 | `max(4, 总枚数) − \|S\| + \|S\|²` | 11 |
| 乙 | `max(4, 色块枚数) + \|S\|²` | 13 |
| 丙 | 只要有星星就整组平方 | 25 |

玩家先说「按乙」，接着把真实规则给全了：混合组**一分都不变**（5 分），三种都不
是。乙上线过几分钟就退回来了（`4fffbb4` → 下一笔）。门里那条「只要组里还有一枚
色块，分数就和从前一模一样（扫 72 种组合）」就是那次的疤。

丙当时额外有个否掉的理由，留着有用：`●●●★` 会从 4 分跳到 16 分，玩家只要往任何
图案里塞进一颗星星分就翻四倍，那是刷分入口。现在的规则连这个口都没有。

### 老虎机不受影响

老虎机模式（targets）的图案分走 `src/engine/targets.ts` 的 `scoreOf`
（`ceil(n²/2)`），是另一条路，一个字都没碰。

## 二、规格里核出来的六处，逐条的结论

| | 规格怎么写的 | 核实结果 |
| --- | --- | --- |
| 1 | 「七副棋盘」 | **错，是八副**。漏掉了 `squareDiamond`。`src/shapes/` 下八个模块：square squareDiamond circle circleHex circleSeven triangle triangleBig triangleAdvanced |
| 2 | BLANK 清单 | **漏了 `triangle.ts`**。另外 `circle.ts` 和 `triangleBig.ts` 早就有 `isBlank` 了，不是从零开始 |
| 3 | §1.3 纯星星四连一处 16 一处 20 | **自相矛盾**。按玩家「按星星枚数的平方」的说法，是 **16** |
| 4 | `clearedStars` 的 mask | **不对称，会开刷分洞**。玩家说的「这 5 个消除」正好是那道闸：星星得分后**从棋盘上消失**（留空位），所以同一批星星凑不回同一个形状，不需要另记一本账 |
| 5 | 全局删掉 `scoring.ts` 的 flavor filter（「一组里至少有一枚正面才给分」） | **不能全局删**。那条正是防刷分的闸，而且删掉会改到无限反转，跟玩家说的「无限反转维持原样」冲突。删除要挂在 `!cfg.toggleOnMatch` 上 |
| 6 | D8（V 形仅天才挑战）列为作废 | **错，照旧有效**。它和外边消除无关，见 `外边消除决策.md` |

---

## 三、玩家对四个问题的回答（鱼眼转盘那份规格）

1. 手机端**不**循环
2. 焦点锁定开关**先不做**
3. reduced-motion 下：**只在最后选中一个图标停下来的那一刻**出声，快速滑过的时
   候不播
4. 弹簧系数给一个**能被感知到的小数值**；转满一圈**不提示**

另：主菜单是**整个换掉**（不是在现有三排上加转盘）。

---

## 四、还没做的

- **纯星星组还得不到分**：`groupPoints` 已经接上八副棋盘，混合组按（不变的）老
  公式走，可纯星星组进不了门——`scoring.ts:290` 那道 flavor filter（「一组里至少
  要有一枚正面」）还挡着它，得连着 BLANK（星星得分后消除留空位，见上面第 2、4
  条）一起做。删 filter 要挂在 `!cfg.toggleOnMatch` 上（第 5 条）。
- **排行榜清空**：玩家要求**全部清空**。`api/scores.js` 的 rebuild 加了
  `all: true` 这一路，`public/mint.html` 上对应一个《全部清空》勾选框。它可逆
  （存档不动，再点一次不勾全清的重建就照存档算回来），反过来也是个坑——清完别手
  滑再点普通重建，那会把旧分请回榜上。**这一步要玩家自己去点**（要
  ADMIN_TOKEN）。
- 教学文案、动画、记录页面：都还没动。
