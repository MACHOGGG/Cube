# Slides 微信小游戏上手指南

这份文件回答四件事：做成什么、现有代码能搬多少、你要准备什么、代码骨架怎么用。
和 `iOS上手指南.md`、`后台数据指南.md` 一样，写给第一次做这件事的人看。

> 微信的规则改得勤。下面凡是涉及资质、审核、支付的地方，动手前请以微信公众平台
> 当时的官方说明为准；我标了「据我了解」的地方就是这个意思。

---

## 一、先定三件事

### 1. 做「小游戏」，不做「小程序」

Slides 是游戏。据我了解，微信审核会把游戏内容归到「小游戏」类目，用「小程序」类目
提交会被打回。两者的开发方式也不同：

| | 小程序 | 小游戏 |
|---|---|---|
| 画界面 | WXML/WXSS（像网页） | 没有 DOM，只有一块画布（Canvas） |
| 适合 | 表单、列表、商城 | 棋盘、动画、手势 |
| 我们的棋盘 | 反而难做 | 正合适 |

结论：**做小游戏，用 Canvas 画**。这也是我在骨架里做的。

### 2. 用 TypeScript + 原生小游戏 API，不用 Taro / uni-app / Cocos

- 网页版就是无框架的 TypeScript，规则引擎（计分连锁、死局、老虎机图案判定、随机数）
  一行不改就能拿来用；套一个框架反而多一层要学的东西。
- Cocos / Laya 这类游戏引擎适合 3D 或复杂场景。我们只有二维棋盘、圆角方块、几种
  动画，原生 2D Canvas 够用，而且没有引擎自己的一套编辑器要学。
- 一份 `game.js`，微信里和浏览器里都能跑（浏览器里跑是为了自动测试和截图）。

### 3. 后端用「微信云开发」，不直接连现在的服务器

现在的后端在 Vercel（境外），域名 play-slides.com 没有 ICP 备案。小游戏里
`wx.request` 只能访问**已备案**的 HTTPS 域名，所以现在这套后端小游戏够不着。

两条路：

| 路 | 要做的事 | 周期 |
|---|---|---|
| A. 微信云开发（推荐） | 在小游戏后台一键开通；`api/room.js` 的逻辑改写成一个云函数，Redis 换成云数据库；登录自动带 openid，不用自己做账号 | 几天 |
| B. 国内服务器 + 备案 | 买阿里云/腾讯云主机，域名备案（个人备案要实名、拍照、等审核，通常 2-4 周），把 `api/` 部署上去 | 一个月起 |

推荐 A。代价是多人小屋在网页和小游戏之间不互通（各有各的房间）——两边本来也是不同
的玩家群。以后想互通，再把云函数改成转发到同一个数据库。

---

## 二、现有代码能搬多少

我把网页版的文件按「能不能脱离浏览器跑」分了一遍：

**直接复用（不改一行）**

- `src/engine/scoring.ts` 计分连锁（得分 → 翻面 → 整线 → 再判、连击倍率）
- `src/engine/stalemate.ts` 死局判定
- `src/engine/targets.ts`、`targetMatch.ts` 老虎机的 20 个图案和判定
- `src/engine/rng.ts` 可播种的随机数（多人小屋「同一副棋盘」靠它）
- `src/engine/performance.ts` 有效得分率
- 综合得分公式（`gameController.ts` 里那几行，我抄到了 `wxgame/src/composite.ts`）

**要重写（因为它们直接操作网页的 DOM）**

- 三种基础棋盘的画面与拖动：`src/shapes/square.ts`、`circle.ts`、`triangleBig.ts`
  各 900 行左右，规则和画面写在一起。做法是把规则抄成不带画面的模型（方块的我已经
  做了：`wxgame/src/squareBoard.ts`），画面用 Canvas 重画。
- 所有页面（主菜单、开局倒数、结算、小屋、老虎机页）：`src/ui/` 约 9700 行，只挑
  你要的几屏重做，而且小游戏的屏幕比网页简单得多。
- 多人小屋客户端 `src/engine/room.ts`：请求从 `fetch` 换成 `wx.request` / 云函数
  调用，状态机可以照搬。

**不搬**

- 账号、订阅、内部码、Creem 支付：小游戏里的付费另有规矩（见下一节）。
- 分享战绩图：小游戏有自己的 `wx.shareAppMessage`，图要用 Canvas 重画。

---

## 三、资质与限制（据我了解，请核对官方最新说明）

1. **主体**：个人主体可以注册小游戏。企业主体才能开通微信支付。
2. **软件著作权**：小游戏类目提交审核时要提供《计算机软件著作权登记证书》。自己
   去中国版权保护中心申请免费但要排队（一两个月）；代办加急一两周、几百到一千元。
   这是整个流程里最耗时的一步，**建议现在就开始办**。
3. **付费**：
   - iPhone 上的小游戏不能做虚拟商品支付（苹果的规定），Android 可以走微信支付。
   - 有内购的小游戏据我了解还要《游戏版号》，个人几乎拿不到。
   - 所以第一版小游戏**不做付费**：把「Slides 天才」的内容要么全开放、要么不放进
     小游戏。老虎机、多人这些你打算迁移的内容，在小游戏里就直接开放。
4. **域名**：只有走路 B 才需要备案；走云开发不需要。
5. **审核**：首次提交通常 1-7 天。要准备：图标（大小按后台要求）、截图、类目说明、
   隐私说明（我们不收集个人信息，填「不收集」）。

---

## 四、你要准备的清单

1. 在 [mp.weixin.qq.com](https://mp.weixin.qq.com) 注册小游戏账号（选「小游戏」，
   个人主体需要身份证 + 微信扫码实名）。拿到 **AppID**，发给我（AppID 不是密钥，
   可以放进代码）。
2. 下载安装「微信开发者工具」（稳定版）。
3. 在小游戏后台开通「云开发」（免费额度够开发期用），把环境 ID 发给我。
4. 开始办软著（材料：程序源代码前后各 30 页、软件说明书；我可以按要求导出）。
5. 图标：1024×1024 的正方形 PNG（不带圆角，微信自己裁）。

**不要发给我**：微信后台的 AppSecret、云开发的任何密钥。小游戏本身用不到它们；
将来云函数需要时，是在微信后台配置，不经过聊天。

---

## 五、代码骨架怎么用

仓库里新开了 `wxgame/` 文件夹：

```
wxgame/
  game.js              ← 打包好的入口（微信只认这个文件）
  game.json            ← 小游戏配置（竖屏、不显示状态栏）
  project.config.json  ← 开发者工具的项目文件（AppID 先是游客 ID）
  src/
    main.ts            ← 入口：手指、连锁节拍、读数、结算
    squareBoard.ts     ← 方块 6×6 的规则模型（照 square.ts 抄，不带画面）
    render.ts          ← Canvas 画法：棋盘、棋子、读数、结算卡
    platform.ts        ← 微信 / 浏览器两头的薄壳（画布、触摸、震动）
    composite.ts       ← 综合得分公式（同 gameController）
```

**打开看看**：微信开发者工具 →「导入项目」→ 选 `wxgame` 文件夹 → AppID 用「测试
号」或你自己的 → 打开后模拟器里就是方块基础版：拖一行、得分翻面、整行消掉、全翻
完或死局结算，综合得分和网页版同一条公式。

**改了代码之后**：

```
npm run build:wxgame        # 类型检查 + 打包成 wxgame/game.js
node scripts/check-wxgame.mjs   # 规则回归 + 浏览器里真拖一下 + 截图
```

**现在能玩的**：方块基础版（发牌、滑动、2×2 / 1×4、整线、连击、综合得分、死局）。
还没有的：开局倒数、暂停、音效、粒子、多语言。

---

## 六、后面的顺序

按你说的范围（几个基础版本 + 多人 + 老虎机），建议这样排：

1. **小球、三角的规则模型**（照 `circle.ts` / `triangleBig.ts` 抄成不带画面的模型，
   加各自的 Canvas 画法与拖动手势）。方块的样板已经在了，照着做。
2. **主菜单 + 开局倒数 + 结算 + 再来**：三张卡、4-3-2-1、结算卡。
3. **老虎机模式**：图案判定直接用 `targetMatch.ts`；老虎机转动画用 Canvas 重画。
4. **多人小屋**：开通云开发；`api/room.js` 改成云函数（建屋 / 进屋 / 开局 / 报分 /
   离开），云数据库存房间；客户端状态机照 `src/engine/room.ts` 搬。
5. **提审**：软著到手后，准备图标、截图，提交。

粗略工作量：1 和 2 各两三天，3 一两天，4 三五天，提审材料一天。软著是另一条并行
的线，越早办越好。
