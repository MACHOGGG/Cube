node_modules
dist
dist-artifact
dist-ssr
*.local
.DS_Store
*.tsbuildinfo

# Capacitor / native
android/app/build
android/.gradle
android/local.properties
ios/App/Pods
ios/App/build

# Secrets — API keys live here and never in the repo
.env
.env.*
!.env.example

# 设计画布与图标清单：由 scripts/ui-snapshot.mjs 和 scripts/icon-sheet.mjs
# 从代码里重新生成，不是源文件。
design/
slides-interface.html
slides-icons.zip

# 界面模拟器：由 scripts/ui-screens.mjs + build-simulator.mjs 生成，不是源文件。
slides-simulator.html

# 小红书小工具的出包：由 npm run build:xhs 生成，不是源文件。
xhs/slides-minitool.zip
