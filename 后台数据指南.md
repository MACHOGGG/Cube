# Slides 后台数据指南

网站现在会把使用情况上报到两个后台。代码已经写好并上线，但**有两处需要你在网页控制台点开关**，不点的话面板里是空的。

---

## 一、Vercel Web Analytics（必做，5 分钟）

这是主力后台：不用 cookie、不用写隐私声明、数据发到你自己的域名下，**中国大陆也能正常上报**。

### 你要做的

1. 打开 https://vercel.com → 进入 `slides` 项目
2. 顶部选 **Analytics** 标签
3. 点 **Enable**（免费额度按月计事件数，具体额度以面板显示为准）
4. 完成。下一次有人访问 play-slides.com 就开始有数据了

### 能看到什么

- 访客数、访问次数、每个页面的浏览量
- 来源（谁把流量带来的：二维码、社交媒体、搜索引擎、直接输入网址）
- 国家 / 地区、设备类型、浏览器、操作系统
- **自定义事件**（下面那一节），在 Analytics 页面下方的 Events 里

---

## 二、Google Analytics 4（可选）

指标比 Vercel 全（自带互动时长、留存队列、漏斗），但有三个代价：**中国大陆打不开**、会写 cookie、按法律需要隐私政策和 cookie 同意条。

代码已经写好了，但**默认是关的**。要打开：

1. 去 https://analytics.google.com 建一个"媒体资源"，数据流选"网站"，网址填 `https://play-slides.com`
2. 拿到那个 **衡量 ID**，长这样：`G-XXXXXXXXXX`
3. 回到 Vercel → `slides` 项目 → **Settings → Environment Variables**
4. 新建一条：
   - Name：`VITE_GA_ID`
   - Value：你的 `G-XXXXXXXXXX`
   - Environment：勾 **Production**
5. 保存后**必须重新部署一次**才会生效（Deployments 页面最新那条右边 `⋯` → Redeploy）

> 没设这个变量时，GA 的代码一行都不会运行，也不会向 Google 发任何请求。

### 大陆访问不会被拖慢

已经实测过：把 googletagmanager.com 完全阻断（模拟大陆网络），
- 游戏照常渲染、照常可交互，没有白屏
- 没有任何 JS 报错
- Vercel 那一路的统计不受影响，继续正常上报

因为 GA 的脚本是 `async` 加载的，加载失败也只是让排队的事件留在内存里，永远不会挡住画面。

### ⚠️ 上线前你还需要补

GA4 会写 cookie。**如果有欧盟访客**，法律上需要：
- 一份隐私政策页面
- 一个 cookie 同意条（同意前不加载 GA）

这两样现在都还没有。我可以下一步做，你说一声。只用 Vercel 那一路的话不需要这些。

---

## 三、现在会上报哪些事件

所有事件同时发到 Vercel 和（如果开了）GA4。**不收集任何个人信息**——没有姓名、没有邮箱、没有任何玩家输入的文字，每一个字段要么是我们代码里固定的枚举值，要么是数字。

| 事件 | 什么时候发 | 带的信息 | 能回答什么问题 |
|---|---|---|---|
| `session_start` | 每次打开网站 | 第几次访问、是否回访、距首次/上次多少天、语言、屏幕类别 | 有多少人回来？留存曲线长什么样？ |
| `session_end` | 关闭或切走标签页 | 停留秒数、时长档位、看了几个页面、玩了几局 | **平均停留多久**？看了就走还是真的玩了？ |
| `screen_view` | 每次切换界面 | 界面名（menu/profile/records/tutorial/…） | 哪些页面真的有人去？ |
| `game_start` | 点《开始游戏》 | 玩法、模式 | **哪个玩法最受欢迎**？ |
| `game_end` | 一局结束 | 玩法、模式、得分、步数、秒数、结束原因、是否被炸弹结束 | 平均一局多久多少分？大家都是怎么输的？ |
| `tutorial_start` | 教学开始播放 | 哪个形状 | 有多少人看教学？ |
| `tutorial_end` | 点《完成》或《结束》 | 是否看完、**停在第几条 / 共几条 / 完成百分比** | **教学在第几条把人劝退了**？ |
| `language_set` | 选定语言 | 语言、是首次选择还是后来切换 | 玩家实际都是哪国人？ |
| `share_open` | 打开分享战绩图 | 从结算弹窗还是从记录页 | 分享功能有人用吗？ |
| `icon_change` | 换标签页图标 | 换成了哪个 | 11 个图标里哪个最受欢迎？ |

### 一句话看懂"浏览时长"

`session_end` 的 `seconds` 只算**页面真正可见的时间**——标签页丢在后台挂一小时不算一小时。同时给了一个 `bucket` 分档（`0-10s` / `10-30s` / `30-60s` / `1-3m` / `3-10m` / `10-30m` / `30m+`），因为后台按"347 秒"分组是没法看的，按档位才能一眼看出分布。

---

## 四、哪些情况下完全不上报

- **artifact 版本 / 本地打开的副本**（`file://`）：一个字节都不发
- **本机开发**（localhost / 127.0.0.1）：不发，免得你自己调试的流量污染真实数据
- 只有真实的 https 域名上才会上报

已实测：用 `file://` 打开完整走一遍流程，外部请求 0 个、事件 0 条、报错 0 个。

---

## 五、排行榜维护：把某一种玩法从榜上抹掉

一局报上来的时候，服务器记的 `mode` 是**棋盘**（square / circle / triangle…），
玩法本身（基础 / 计时 / 炸弹 / 无限反转）只写在那一局的存档里。所以无限反转打
出来的分和基础方块记在**同一张榜**上——它早先那版计分能打到七位数，榜上于是
出现一排削到上限的同一个数。

要把某一种玩法从榜上抹掉，用这一条（`ADMIN_TOKEN` 就是 Vercel 后台里那一个，
别贴进聊天记录）：

```bash
curl -X POST https://play-slides.com/api/scores \
  -H 'Content-Type: application/json' \
  -d '{"action":"sweep","token":"这里填 ADMIN_TOKEN","modeKey":"flip"}'
```

`modeKey` 可以填 `flip`（无限反转）、`timed`（计时）、`bomb` / `bombTimed`
（炸弹）。回包长这样：

```json
{"ok":true,"modeKey":"flip","players":128,"touched":9,"rowsChanged":6,"rowsDropped":3}
```

`touched` 是动了几个人，`rowsChanged` 是改低了几行，`rowsDropped` 是整行撤下来
几行（那个棋盘上他只打过这一种局）。它做的事：

- 只动**榜**：把每个人被污染的那个棋盘的最高分，改成他存档里同一棋盘上「不算
  这一种局」的最高分；一局都没有就把他从那张榜上撤下来；总榜跟着重算。
- **不动**他自己的存档和累计得分——那是他的记录，不是榜。
- 可以重复跑：抹干净之后再跑一次，`touched` 就是 0。

一个已知的边界：云端存档只留每人最近 60 局，更早的那些翻不出来，也就无从判断
是哪一种局。所以这条命令按「翻得到的」算。
