# 《外边消除 + 能消的都消完》—— 玩家拍板的决策

这一份只记**决定**，不重复方案（方案见交接文档《Slides 外边消除 + 能消的都消完 · 实施方案
v1.1》第 7 节 D1–D10）。

为什么要有这个文件：这套改动按方案切成 7 个 PR，横跨引擎、八副棋盘、四种语言的文案、
图标分镜、记录与排行、小屋协商、炸弹变体、小红书重打包。决策在第一个 PR 拍的，代码在
第六个 PR 才写——中间隔着好几轮会话。口头说过的决定不落地成文件，到那时一定走样。

一条决定被实现之后，实现处的注释里要点名它（这个仓库的规矩：注释解释「为什么」）。

---

> ## ⚠️ 2026-09-19：《外边消除》这套方案已取消
>
> 玩家改走**《星星跟随色块消除》**（实施指令 v1.0）：星星不再靠「凑满棋盘此刻最外面
> 的一整条边」消除，而是「一组得分图案里、单看星星那部分自己也构成图案，那部分就消
> 掉，变成空图形留在原位」。整行/整列消除整条路径一并取消，棋盘也不再一圈圈收缩。
>
> **这个文件保留下来，因为它记的决定有一多半和外边无关，至今仍然有效。** 下面逐条标
> 注哪些还算数、哪些作废——不要整份当废纸，也不要整份当现行规则。
>
> 代码那边：`scoring.ts` 的 `outerEdges`（三条判定的共用实现）、`scripts/check-outer-edges.mjs`
> 和 CI 里《此刻的最外边》那一步，都在《星星跟随色块消除》PR-1 里删掉了。
>
> | 决定 | 现在算不算数 |
> | --- | --- |
> | **D1a** 冲击波（消边也算得分事件，拆旁边的炸弹） | **字面作废**——没有「消边」了。但它的意思延续下来了：新规则里**星星消除同样算得分事件，同样拆旁边的炸弹**（实施指令 §3.2c、验收第 8 条） |
> | **D1b** 永久炸弹星取消 | **待重议——还没实施，而且它的理由和数据都已经不成立。**代码现状：`src/engine/bomb.ts` 的 `dealBombBacks` 第一张反面仍然是红色（`const backs: number[] = [redIdx]`），`BOMB_RULES_VERSION` 仍然是 `2`（D1b 落地要升到 3）。**不要照这一格直接去改代码**，理由见下面《D1b 的读法》 |
> | **D2** 六边三角 24 的 4 色 | **仍然有效** |
> | **D3** 三个时长全部 100 秒 | **仍然有效**（还没实施：现在计时 60、定时炸弹 90、无限反转 60） |
> | **D4** 大棋盘收尾期综合分下降 | **作废**——它的前提是「棋盘一圈圈变小」，现在不缩了 |
> | **D6a** 旧战绩保留并标注、不进新榜 | **作废**——新规则的 D13 是**全部清空**（本地 + 云端） |
> | **D6b** 在途局收进旧榜 | **作废**，同上 |
> | **D7** 《提示》按钮只在教学与首局 | **仍然有效** |
> | **D8** V 形维持，仅天才挑战 | **仍然有效**（和外边无关——实施指令里把它列进作废那一档是写错了） |
> | **D9** 三角只剩一副 | **仍然有效** |
> | **D10** 三角图标底色 | **仍然有效** |
>
> 另外两处跟着失效：
>
> - 下面《D1b 的读法》里那句「`bomb.ts` 那个理由在外边消除规则下本来就不成立——现在
>   棋盘本来就会一圈一圈变小」——**新规则下棋盘不缩**（方块永远 6×6，只是空图形越来越
>   多），所以那个理由重新成立。
>
>   **顺着往下推，D1b 整条就悬空了**：`bomb.ts` 保留那一颗永久炸弹星原本是有理由的
>   （玩家当初定的，注释里写着），D1b 推翻它靠的正是「外边消除下棋盘会缩小，那个理由
>   不成立」。外边消除取消了，理由回来了。
>
>   而且 D1b 的**支撑数据也过期了**：那组「终局活炸弹 0.26 枚、残留 20%、综合分 1639」
>   是在外边消除规则下跑出来的模拟，现在不能拿来当依据。
>
>   所以 D1b 现在需要的是**玩家在新规则下重新拍板**，不是照旧去补代码。要重新比较，
>   用新规则重跑一次模拟，别再引那组 1639。
> - 下面《D6a / D6b 要落到哪里》里的 `RULES_VERSION = 'peel1'`——改用实施指令 §5 的
>   `SCORING_RULES_VERSION = 2`。
>
> 《D3 带出来的一件事：无限反转的封顶（D5）》那一节**不受影响**，仍然没拍板：新规则的
> D11 明写无限反转维持原样。

---

## 已拍板（2026-09-19，玩家）

| # | 事项 | 决定 |
|---|---|---|
| **D1a** | 冲击波（消边也算得分事件，贴着被消那条边的立着的炸弹各挨一下） | **要** |
| **D1b** | 永久炸弹星 | **取消** |
| **D2** | 六边三角 24 的 4 色 | 取三角配色表**前 4 个**（`#2F8A96 #B23A3A #D89B1E #4C68B0`），色盲配色同样取前 4 个 |
| **D3** | 三个时长 | **全部 100 秒**：计时挑战 60→100、定时炸弹 90→100、无限反转 60→100 |
| **D6a** | 旧规则的战绩 | **保留并标注，不进新榜** |
| **D6b** | 部署那一瞬间的在途局 | **收进旧榜，并提示玩家** |
| D4 | 大棋盘收尾期综合分下降 | 按方案默认：先保持，结算页提示峰值 |
| D7 | 《提示》按钮 | 按方案默认：只在教学与首局 |
| D8 | V 形 | 维持，仅天才挑战（方案已定） |
| D9 | 三角只剩一副 | 已定（六边 54 删除、两份引擎合并、进阶炸弹只剩两副） |
| D10 | 三角图标底色 | 灰（与方块、圆球统一），图案延续现在六边三角那一枚 |

---

## D1b 的读法（实现前请确认这一条）

「永久炸弹星 取消」有两种读法，我按**第一种**实现：

1. **炸弹星这个东西本身取消** —— `dealBombBacks` 不再放红色反面，拆掉的炸弹一律变成基础
   色星星。这正是方案 §7.1 里的「无永久星」那一档；和 D1a 叠起来就是方案自己的**备选推荐
   「无永久星 + 冲击波」**（实测终局活炸弹 0.26 枚、有炸弹残留 20%、综合分 1639 最高，是
   数据上最干净的一档）。
2. 另一种读法是「『永久星搭边』这个变体不做」，那样留下的是现版 v2 + 冲击波（终局活炸弹
   1.23、残留 100%）。

按第一种做的**代价**要写明：v2 那句设定「场上始终有一枚永久威胁」就没有了。不过方案里也
指出，`bomb.ts` 里「棋盘不会被清空成普通局」这个理由**在外边消除规则下本来就不成立**——
现在棋盘本来就会一圈一圈变小。所以那段注释要跟着重写，不是只改代码。

威胁没有被拿掉：盲目贪心的爆炸率仍然 14%（v2 是 16%），失败依旧可归因于玩家自己把炸弹
滑到一起。

---

## D3 带出来的一件事：无限反转的封顶（D5）从「另开题」变成基本必做

无限反转的连击是 **1.5ⁿ**（`scoring.ts` 的 `flipStreakDelta`：`round(points × 1.5^chain)`，
`chain` 是这一次之前已经连续得了几次分）。

> **⚠️ 2026-09-25 更正：这一节原先写的「到第 32 步倍率已经 ≈1.4×10¹⁵」按现行公式重现不
> 出来，不要再引它。** 照 `flipStreakDelta` 逐项实算（node）：
>
> | 连续第几次得分 | 倍率 |
> | --- | --- |
> | 10 | ×38 |
> | 20 | ×2.2×10³ |
> | **32** | **×2.9×10⁵** |
> | 40 | ×7.4×10⁶ |
> | 57 | ×7.3×10⁹ |
>
> 要到 1.4×10¹⁵ 得连续**约 87 次**，差了将近十个数量级。
>
> **但溢出这件事本身仍然是真的**：4 分的图案，大约**连续第 49 次**时单次得分就超过十亿
> （实算 1.134×10⁹）。所以这一节的结论照旧成立，只是那个吓人的数字不对。
>
> 另外「60 秒拼出 57 个图案」出自**《外边消除》方案**的附录 B.5，规则已经换过一轮，这个
> 数也要在新规则下重测（脚本：`scripts/sim-flip-streak.mjs`）。封顶封到多少，取决于重测
> 出来的分布，**别直接抄这一节建议的 n ≤ 10**。

**改成 100 秒之后步数大约 ×1.6**，倍率跟着指数往上走。而排行榜有上限
（`api/scores.js` 的 `MAX_SCORE = 1e9`）：超过就一律记成十亿。于是榜首那一批会显示**同一个
数字**——这正是这个仓库已经修过一次的那个 bug（玩家原话「过了上限以后都按照同一数字显示
了」，见任务 #427）。

所以 D5 不再是「以后再说」：和 D3 同一个 PR 落地。**但它还没拍板，而且拍板的人只能是
玩家**——封顶本身就是在改无限反转的规则，而《星星消除决策》的 D11 明写「无限反转维持原
样」，两者是冲突的。要问的那一句：「100 秒下约有 X% 的局会顶到十亿、在榜上显示同一个
数，要不要封顶？」X 由 `scripts/sim-flip-streak.mjs` 给。

真封顶的时候：`flipStreakDelta` 里 `chain` 取 `Math.min(chain, FLIP_STREAK_CAP)`，
`FLIP_STREAK_CAP` 由重测的数定；封顶是规则变更，照 `BOMB_RULES_VERSION` 的办法开一个
新版本号，让新旧成绩分开排榜。

---

## D6a / D6b 要落到哪里

- **新规则版本号 `RULES_VERSION = 'peel1'`**（照 `BOMB_RULES_VERSION` 的做法）：本地
  `<bestKey>_peel1`、服务器榜 `square:peel1` 等；`RunData.rules` 带版本。
- **D6a**：旧记录留在《记录与排名》里，行上标一句「旧规则」；`api/scores.js` 的新榜只收
  带 `peel1` 的局，旧榜原样归档不删（和 `bomb` → `bomb2` 那次同一条路）。
- **D6b**：一局在旧代码下开始、在新代码下交卷，它玩的是旧规则，所以**按旧版本收进旧榜**，
  并在结算页说明一句。判据是这一局自己带的 `rules` 字段，不是服务器当下的版本。
- 炸弹另有自己的版本号：D1a + D1b 是规则变更，`BOMB_RULES_VERSION` 递增到 3
  （本地 `_bomb3`、服务器榜 `square:bomb3`），旧榜归档。
