# Slides 微信小游戏上手指南

这份文件回答五件事：做成什么、现有代码能搬多少、有了 AppID 之后一步一步怎么做、
「全部免费 + 累计得分兑换天才码」怎么设计、代码骨架怎么用。和 `iOS上手指南.md`、
`后台数据指南.md` 一样，写给第一次做这件事的人看。

> 微信的规则改得勤。下面凡是涉及资质、审核、备案的地方，动手前请以微信公众平台
> 当时的官方说明为准；我标了「据我了解」的地方就是这个意思。官方入口：
> 小游戏开发指南 https://developers.weixin.qq.com/minigame/dev/guide/
> ，公众平台后台 https://mp.weixin.qq.com 。

**现状（2026-09）**：小游戏账号已注册，AppID `wx9cab5874e6df2aee` 已经填进
`wxgame/project.config.json`。AppID 不是密钥，放在代码里没关系；**AppSecret 和云开发
的任何密钥不要发到聊天里**，小游戏本身用不到它们。

---

## 一、先定三件事

### 1. 做「小游戏」，不做「小程序」

Slides 是游戏。据我了解，微信审核会把游戏内容归到「小游戏」类目，用「小程序」类目
提交会被打回。两者的开发方式也不同：

| | 小程序 | 小游戏 |
|---|---|---|
| 画界面 | WXML/WXSS（像网页） | 没有 DOM，只有一块画布（Canvas） |
| 适合 | 表单、列表、商城 | 棋盘、动画、手势 |
| 我们的棋盘 | 反而难做 | 正合适 |

结论：**做小游戏，用 Canvas 画**。这也是我在骨架里做的。

### 2. 用 TypeScript + 原生小游戏 API，不用 Taro / uni-app / Cocos

- 网页版就是无框架的 TypeScript，规则引擎（计分连锁、死局、老虎机图案判定、随机数）
  一行不改就能拿来用；套一个框架反而多一层要学的东西。
- Cocos / Laya 这类游戏引擎适合 3D 或复杂场景。我们只有二维棋盘、圆角方块、几种
  动画，原生 2D Canvas 够用，而且没有引擎自己的一套编辑器要学。
- 一份 `game.js`，微信里和浏览器里都能跑（浏览器里跑是为了自动测试和截图）。

### 3. 后端用「微信云开发」，不直接连现在的服务器

现在的后端在 Vercel（境外），域名 play-slides.com 没有 ICP 备案。小游戏里
`wx.request` 只能访问**已备案**的 HTTPS 域名，所以现在这套后端小游戏够不着。

两条路：

| 路 | 要做的事 | 周期 |
|---|---|---|
| A. 微信云开发（推荐） | 在小游戏后台一键开通；`api/room.js` 的逻辑改写成一个云函数，Redis 换成云数据库；登录自动带 openid，不用自己做账号 | 几天 |
| B. 国内服务器 + 备案 | 买阿里云/腾讯云主机，域名备案（个人备案要实名、拍照、等审核，通常 2-4 周），把 `api/` 部署上去 | 一个月起 |

推荐 A。代价是多人小屋在网页和小游戏之间不互通（各有各的房间）——两边本来也是不同
的玩家群。以后想互通，再把云函数改成转发到同一个数据库。

**云函数可以访问境外域名**（那条「只能访问备案域名」的规矩管的是小游戏客户端，不管
云函数）。第四节的积分兑换就靠这一点：云函数替玩家去 play-slides.com 换一张天才码。

---

## 二、现有代码能搬多少

我把网页版的文件按「能不能脱离浏览器跑」分了一遍：

**直接复用（不改一行）**

- `src/engine/scoring.ts` 计分连锁（得分 → 翻面 → 整线 → 再判、连击倍率）
- `src/engine/stalemate.ts` 死局判定
- `src/engine/targets.ts`、`targetMatch.ts` 老虎机的 20 个图案和判定
- `src/engine/rng.ts` 可播种的随机数（多人小屋「同一副棋盘」靠它）
- `src/engine/performance.ts` 有效得分率
- 综合得分公式（`gameController.ts` 里那几行，我抄到了 `wxgame/src/composite.ts`）

**要重写（因为它们直接操作网页的 DOM）**

- 三种基础棋盘的画面与拖动：`src/shapes/square.ts`、`circle.ts`、`triangleBig.ts`
  各 900 行左右，规则和画面写在一起。做法是把规则抄成不带画面的模型（方块的我已经
  做了：`wxgame/src/squareBoard.ts`），画面用 Canvas 重画。
- 所有页面（主菜单、开局倒数、结算、小屋、老虎机页）：`src/ui/` 约 9700 行，只挑
  你要的几屏重做，而且小游戏的屏幕比网页简单得多。
- 多人小屋客户端 `src/engine/room.ts`：请求从 `fetch` 换成云函数调用，状态机可以照搬。

**不搬**

- 账号、订阅、内部码输入、Creem 支付：小游戏里全部免费（见第四节），这些都不需要。
- 分享战绩图：小游戏有自己的 `wx.shareAppMessage`，图要用 Canvas 重画。

---

## 三、有了 AppID 之后，一步一步做

官方指南的顺序是：注册 → 开发者工具 → 写代码 → 预览/真机调试 → 上传 → 体验版 →
后台配置 → 提交审核 → 发布。你已经做完第一步。下面每一步都写清「在哪儿点、要填什么、
会看到什么」；带 ★ 的是我做，其余是你做（后台只有你的微信能登）。

### 第 1 步　装微信开发者工具，登录

1. 打开 https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html ，
   下载**稳定版**（Stable），macOS / Windows 各有一个。装好打开。
2. 用注册这个小游戏账号时绑定的微信扫码登录。第一次会问「信任此电脑」，点信任。

### 第 2 步　导入项目

1. 工具首页左栏选「小游戏」→ 右边「导入」。
2. 目录：选仓库里的 `wxgame` 文件夹（**不是仓库根目录**）。
3. AppID：会自动读到 `wx9cab5874e6df2aee`；如果没有，手动填。别选「测试号」。
4. 后端服务：先选「不使用云服务」。第三节末尾开通云开发时再改。
5. 点「确定」。模拟器里应该直接出现方块棋盘：拖一行、得分翻面、整行消掉、全翻完或
   死局结算。这就是当前的方块基础版原型。

出问题最常见的两个：
- 「基础库版本不支持」——工具右上角「详情」→「本地设置」→「调试基础库」选最新的那个
  （`project.config.json` 里写的是 `latest`，工具有时要你手点一次）。
- 「不在合法域名列表」——现在没有任何网络请求，不该出现；出现了说明选错了项目。

### 第 3 步　真机上玩一玩

1. 工具顶栏「预览」→ 出二维码 → 用同一个微信扫 → 手机上直接打开。这是最快的试玩方式。
2. 「真机调试」→ 手机上打开后，工具里能看到手机端的报错和日志。拖动手感、翻面动画
   都在这一步看。
3. 想给别人玩：要先做第 5 步的体验版，预览码只有你自己能扫。

### 第 4 步　填后台的基本资料（在 mp.weixin.qq.com）

登录后台，左栏「设置」→「基本设置」：

| 项 | 填什么 |
|---|---|
| 小游戏名称 | 建议就叫 `Slides`（名称一年只能改两次，想好再填；如果 Slides 被占了，可以加一个词，比如「Slides 方糖」） |
| 小游戏头像 | 1024×1024 正方形 PNG（不带圆角，微信自己裁）。可以用 `public/` 里的应用图标（★ 我可以按尺寸导出） |
| 介绍 | 一句话，比如「滑动、得分、翻面——方块、小球、三角三种棋盘的消除解谜」 |
| 服务类目 | **游戏 → 休闲游戏**（据我了解「休闲 / 益智」不要求版号；带虚拟支付的才要。我们不做支付，见第四节）。类目页会列出这一类要交的材料，以页面上写的为准 |

左栏「开发」→「开发管理」→「开发设置」：
- 「服务器域名」：走云开发不用填。
- 「业务域名」：不用。

左栏「设置」→「基本设置」里还有两项，审核时会看：
- **用户隐私保护指引**：我们不收集任何个人信息（云开发的 openid 是微信自己给的、匿名
  的），选「不收集」/「不处理用户信息」一路提交即可。将来做云端排行要显示昵称时再改。
- **适龄提示**：小游戏要求填。这个游戏没有任何不适合的内容，按提示选最低档即可。

### 第 5 步　上传代码，做体验版

1. 工具右上角「上传」→ 填版本号（第一次 `0.1.0`）和一句项目备注 → 上传。
2. 后台左栏「管理」→「版本管理」→「开发版本」里会出现它 → 点「选为体验版」。
3. 「成员管理」→「体验成员」里加你朋友的微信号（最多几十个），他们扫体验版二维码
   就能玩。**这一步就是你说的「试水」**：先给一圈人玩，看反馈，再决定要不要走审核。

### 第 6 步　小程序备案（发布前必须，据我了解）

2023 年 9 月起，新注册的小程序 / 小游戏**要先完成备案才能发布**（体验版不需要）。
在后台左栏「设置」→「基本设置」→「小程序备案」按流程填：主体信息（个人：身份证、
手机号、承诺书）、小程序信息（名称、服务内容、类目）。微信初审几个工作日，再交给
通信管理局审核，通常 1-3 周。**这一步和第 5 步的试水可以并行**，越早交越好。

### 第 7 步　提交审核（试水结果不错之后）

后台「版本管理」→ 开发版本那一行「提交审核」：
1. 版本描述：写清楚这一版能玩什么（「方块 / 小球 / 三角三种基础玩法、老虎机模式、
   无限反转、多人小屋；全部免费，无内购」）。
2. 《自查报告》：这是一份勾选清单（内容合规、无诱导分享、无收集手机号、无未申报的
   支付、隐私指引已填……），照实勾。我们没有支付、没有登录、没有广告，基本都是「否」。
3. 测试账号：不需要（没有登录）。
4. 截图：手机竖屏截 3-5 张（主菜单、一局进行中、结算）。★ 我可以用自动化脚本截。
5. 软著：**休闲 / 益智类、无虚拟支付，据我了解现在不强制**。提交时如果类目页要求上传
   《计算机软件著作权登记证书》，再去办（自己申请免费但排队一两个月；代办一两周、几百
   到一千元）。所以先不办，等后台真的要再说。

审核通常 1-7 天。被打回会写明原因，改了再提交，不限次数。

### 第 8 步　发布

审核通过后「版本管理」→「审核版本」→「发布」。可以「全量发布」，也可以先「分阶段
发布」给一部分用户。发布后微信搜索就能找到。

### 之后：开通云开发（多人小屋、累计得分要用）

后台左栏「开发」→「云开发」→ 开通（个人主体也能开；有免费额度，试水期够用）→ 得到
一个**环境 ID**（形如 `slides-xxxx`），把它发给我（环境 ID 不是密钥）。然后在开发者
工具里把「后端服务」改成「微信云开发」。云函数的代码在 `wxgame/cloud/`，由工具右键
「上传并部署」。

---

## 四、全部免费，和「累计得分换天才码」

### 免费

小游戏版是试水，**所有内容全部免费**：三种基础玩法、老虎机模式、无限反转、多人小屋
都不上锁，不接任何支付。这样也躲开了小游戏里最麻烦的三件事：iPhone 上不能做虚拟支付、
带内购要版号、个人主体开不了微信支付。

### 累计得分兑换（为「反馈好」做的准备）

玩家在小游戏里每打完一局，综合得分累计到他的账户上（按微信 openid，不用注册）。攒够
了可以换正规版（网页 / App）的《Slides 天才内部码》：

| 累计得分 | 换到的内部码 | 对应 `api/mint` 的 plan |
|---|---|---|
| 10 000 | Slides 天才 1 个月 | `month` |
| 20 000 | 半年 | `half` |
| 30 000 | 1 年 | `year` |
| 40 000 | 终身 | `life` |

换到的码在网页 / App 的「个人主页 → Slides 天才内部码」里输入，和现在的内部码完全一
样，不涉及钱。反馈好的话，玩家会开始囤分换正规版的权限——这就是它的用意。

**规矩（写死在云函数里，客户端改不了）**
- 得分只能由云函数 `bankRun` 记账：客户端只报「这一局的种子、玩法、步数、用时、
  得分」，云函数按玩法的分数上限和用时下限做合理性检查，通过才累计；每个 openid 每小时
  最多记 60 局。
- 兑换扣的是「累计 − 已花」，一次只能换一档；终身那一档每个 openid 只能换一次。
- 扣分和取码是一件事：先扣、再去取码，取不到就把分退回去（和网页版兑换码「烧掉却没
  到账就放回去」同一个原则）。
- 换出去的码存一份在云数据库 `redemptions` 里，人丢了可以在「我的兑换」里再看。

**怎么取码**：云函数调用现有的 `POST https://play-slides.com/api/mint`
（`{ token, plan, count: 1 }`，返回 `{ codes: [...] }`），拿到一张码。`token` 就是
Vercel 上那个 `ADMIN_TOKEN`：**只放在云开发控制台 → 云函数 → 配置 → 环境变量里**，
不进代码、不进聊天。

**代码草案**在 `wxgame/cloud/`（`bankRun`、`redeem` 两个云函数）。它们要等开通云开发、
在工具里部署之后才能跑，这里没法测；接口和数据结构写好了，到时候按文件里的说明部署。

---

## 五、代码骨架怎么用

仓库里的 `wxgame/` 文件夹：

```
wxgame/
  game.js              ← 打包好的入口（微信只认这个文件）
  game.json            ← 小游戏配置（竖屏、不显示状态栏）
  project.config.json  ← 开发者工具的项目文件（AppID 已填，基础库 latest）
  src/
    main.ts            ← 入口：手指、连锁节拍、读数、结算
    squareBoard.ts     ← 方块 6×6 的规则模型（照 square.ts 抄，不带画面）
    render.ts          ← Canvas 画法：棋盘、棋子、读数、结算卡
    platform.ts        ← 微信 / 浏览器两头的薄壳（画布、触摸、震动）
    composite.ts       ← 综合得分公式（同 gameController）
  cloud/
    README.md          ← 云函数怎么部署
    bankRun/           ← 记一局：合理性检查 + 累计得分
    redeem/            ← 换码：扣分 → 取码 → 存档，失败退回
```

**改了代码之后**：

```
npm run build:wxgame            # 类型检查 + 打包成 wxgame/game.js
node scripts/check-wxgame.mjs   # 规则回归 + 浏览器里真拖一下 + 截图
```

**现在能玩的**：方块基础版（发牌、滑动、2×2 / 1×4、整线、连击、综合得分、死局）。
还没有的：开局倒数、暂停、音效、粒子、多语言、小球、三角、老虎机、多人。

---

## 六、后面的顺序

按你说的范围（几个基础版本 + 多人 + 老虎机 + 无限反转，全部免费），建议这样排：

1. **小球、三角的规则模型**（照 `circle.ts` / `triangleBig.ts` 抄成不带画面的模型，
   加各自的 Canvas 画法与拖动手势）。方块的样板已经在了，照着做。
2. **主菜单 + 开局倒数 + 结算 + 再来**：三张卡、4-3-2-1、结算卡。
3. **老虎机模式 + 无限反转**：图案判定直接用 `targetMatch.ts`；老虎机转动画用 Canvas
   重画；无限反转就是方块 / 小球模型加「翻回去」和 60 秒。
4. **云开发**：开通；`bankRun` / `redeem` 部署；`api/room.js` 改成云函数（建屋 / 进屋 /
   开局 / 报分 / 离开），云数据库存房间；客户端状态机照 `src/engine/room.ts` 搬。
5. **兑换页**：累计得分、四档按钮、换到的码 + 一键复制、我的兑换。
6. **备案 + 体验版试水**（第三节第 5、6 步，可以和 1-5 并行）。
7. **提审、发布**。

粗略工作量：1 和 2 各两三天，3 一两天，4 三五天，5 一天，提审材料一天。备案是另一条并
行的线，越早交越好。
